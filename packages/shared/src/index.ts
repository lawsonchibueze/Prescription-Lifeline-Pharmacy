// Shared TypeScript types/DTOs used by both `apps/web` and `apps/api`.
//
// This is intentionally empty for Phase 1 (project scaffolding). Real domain
// types (Product, Category, Order, OrderItem, CartItem, etc.) will be added
// here in Phase 2 once the Prisma schema is defined, so both the frontend and
// backend can share a single source of truth instead of duplicating types.

export const SHARED_PACKAGE_NAME = "@prescription-lifeline/shared";
